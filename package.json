{
  "name": "input-mask",
  "version": "0.0.0",
  "description": "How to create mask to your inputs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "and",
  "license": "ISC",
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^21.0.7"
  },
  "dependencies": {
    "jquery-mask-plugin": "^1.14.16"
  }
}
